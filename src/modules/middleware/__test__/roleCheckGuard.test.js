// const { roleCheckGuard } = require('../roleCheckGuard');
const test = 'testing';

describe('collect data to check role permissions', () => {
  it('admin has permissions to assign new admin', () => {
    console.log(test);
    expect(test).toBe('testing');

  });
});
